# Mr. Luigi Backend - Variables de entorno
# Copiar a .env y ajustar valores

PORT=3007
NODE_ENV=development
FRONTEND_URL=http://localhost:5507

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mr_luigi_db
DB_USER=mr_luigi_user
DB_PASSWORD=tu_password_seguro

# JWT
JWT_SECRET=cambiar_por_cadena_larga_y_segura_mr_luigi_2026
JWT_EXPIRE=8h
JWT_EXPIRES_ADMIN=8h
JWT_EXPIRES_CAJERO=2h

# Admin seed (primera ejecución: npm run seed)
ADMIN_EMAIL=admin@mrluigi.local
ADMIN_PASSWORD=MrLuigi2026Admin
ADMIN_NOMBRE=Administrador Mr. Luigi

# WhatsApp (tienda pública)
WHATSAPP_NUMBER=5734900123456

# Uploads
MAX_FILE_SIZE=5242880
ALLOWED_EXTENSIONS=jpg,jpeg,png,webp,gif

# Backups
BACKUP_PATH=./backups
BACKUP_RETENTION_DAYS=30
